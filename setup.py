from setuptools import setup

setup(version='1.0.0',
      install_requires=[
          'configparser',
          'pyping',
          'httplib2',
          'oauth2client',
          'apiclient',
          'google-api-python-client'
      ],
      )